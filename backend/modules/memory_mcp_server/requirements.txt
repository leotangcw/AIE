# Memory MCP Server Requirements
# Embedding dependencies
FlagEmbedding>=1.2.0  # BGE-M3 embedding model
sentence-transformers>=2.2.0  # Fallback embedder

# Core dependencies (typically inherited from main backend)
# loguru>=0.7.0
# pydantic>=2.0.0
# numpy>=1.24.0
